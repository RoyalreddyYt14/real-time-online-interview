Flask==2.3.3
Flask-SocketIO==5.3.6
Flask-SQLAlchemy==3.0.5
PyPDF2==3.0.1
authlib==1.3.0
requests==2.31.0
# Requires Python 3.11 or 3.12 on Windows. Python 3.14 is not supported by the pinned OpenCV/NumPy stack.
numpy==1.26.4
opencv-python==4.8.1.78
ultralytics==8.4.37
Werkzeug==2.3.7
python-socketio==5.8.0
python-engineio==4.7.1
eventlet==0.33.3
gunicorn==21.2.0
# Audio/Voice Recognition Dependencies
SpeechRecognition==3.10.0
pyttsx3==2.90
# Audio enhancement for low-voice detection
pydub==0.25.1
# PyAudio is required for microphone input. On Windows it often fails
# to build from source — use pipwin to install the prebuilt wheel:
#   pip install pipwin
#   pipwin install pyaudio
pyaudio==0.2.13
# Optional: Noise reduction for enhanced voice detection (pip install noisereduce)
# noisereduce==3.0.0  # Uncomment if needed, requires librosa
# Gmail OAuth2 (XOAUTH2) Support
google-auth==2.29.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0